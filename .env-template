# Environment Variables Template for INE Alumni Backend
# Copy this file to .env and update with your actual values
# DO NOT commit .env file (it's in .gitignore)

# ============================================
# Database Configuration
# ============================================
# For Docker Compose: Use service name 'postgres' as host
# For local development: Use 'localhost'
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ine
SPRING_DATASOURCE_USERNAME=hatim
SPRING_DATASOURCE_PASSWORD=hatim

# ============================================
# JWT Configuration
# ============================================
# Generate a strong secret for production (min 256 bits recommended)
# You can generate one using: openssl rand -base64 32
APP_JWT_SECRET=local-dev-jwt-secret-key-change-in-production-min-256-bits-required-for-security
APP_JWT_EXPIRATION_MS=86400000

# ============================================
# Email Configuration (SMTP)
# ============================================
# For development, you can use a test email service or leave these as placeholders
# For Gmail, you'll need an App Password (not your regular password)
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your-email@gmail.com
SPRING_MAIL_PASSWORD=your-app-password
SPRING_MAIL_SMTP_AUTH=true
SPRING_MAIL_SMTP_STARTTLS_ENABLE=true

# ============================================
# pgAdmin Configuration (Optional)
# ============================================
PGADMIN_DEFAULT_EMAIL=pgadmin@pgadmin.org
PGADMIN_DEFAULT_PASSWORD=admin
